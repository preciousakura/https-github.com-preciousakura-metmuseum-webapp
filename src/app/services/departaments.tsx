import api from "./config/api";
import { callService } from "./config/service";

const service = () => {
  const resource = "public/collection/v1/departaments";

  return {};
};

export const departaments = service();
